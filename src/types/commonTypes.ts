export type SubjectData = {
  name: string;
  day: string;
  start: string;
  end: string;
  classNumber: string;
};

export type EventData = {
  name: string;
  eventDate: string;
};

export type SelectOption = {
  text: string;
  value: string;
};
